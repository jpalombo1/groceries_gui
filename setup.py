from setuptools import setup

setup(
    name="groceries",
    version="1.0",
    description="Grocery List UI",
    author="Joseh Palombo",
    packages=["groceries"],
    setup_requires=["pandas"],
)
